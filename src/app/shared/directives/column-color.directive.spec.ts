import { ColumnColorDirective } from './column-color.directive';

describe('ColumnColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new ColumnColorDirective();
    // expect(directive).toBeTruthy();
  });
});
